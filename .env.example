# 키체인을 쓰는 것이 기본이다 — 이 파일은 키체인을 못 쓰는 환경용 대안이다.
#   security add-generic-password -s naver-clip -a notion-token -w
#
# 이 파일을 쓰려면 .env 로 복사하고 값을 채운다. .env 는 git 에 올라가지 않는다.
# 노션 내부 통합 토큰 — 허브 페이지에 '콘텐츠 업데이트' 권한으로 연결해야 한다.
NOTION_TOKEN=
